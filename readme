all that needs to be installed to run this program is libusb-1.0.
it can be installed with the following command.
sudo apt-get install libusb-1.0-0-dev

Sample main is included but doesn't need to be used.
